
一个字段，以3列或n列的表格显示：

list record

1、通过js拼成json
2、
3、


CREATE TABLE TB_SANT_ (
  ID IDENTITY NOT NULL PRIMARY KEY,
  NAME  VARCHAR(50) NOT NULL,
  DESC  VARCHAR(250) NOT NULL,
  --TYPE_ID INT,
  CONSTRAINT UNIQUE_NAME_1 UNIQUE (NAME)
);

CREATE TABLE TB_SANT_TASK_INFO (
  ID IDENTITY NOT NULL PRIMARY KEY,
  NAME  VARCHAR(50) NOT NULL,
  DESC  VARCHAR(250) NOT NULL,
  CONSTRAINT UNIQUE_NAME_1 UNIQUE (NAME)
);



